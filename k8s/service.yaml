apiVersion: v1
kind: Service
metadata:
  name: didone-identity-platform
  namespace: didone-identity
  labels:
    app.kubernetes.io/name: didone-identity-platform
    app.kubernetes.io/component: identity-runtime
    app.kubernetes.io/part-of: didone-world
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: didone-identity-platform
  ports:
    - name: http
      port: 80
      targetPort: http
